[project]
name = "multimodal-movie-recommender-gnn"
version = "0.1.0"
description = "Multimodal recommendation system using CV, NLP and Graph Neural Networks"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
